import Head from 'next/head';
import styles from '../../styles/Home.module.css';

const Projects = () => (
  <div  className={styles.home}>
    <h1 data-testid="custom-element">Projects page</h1>
  </div>
)

export default Projects;
